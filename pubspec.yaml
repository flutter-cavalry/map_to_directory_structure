name: map_to_directory_structure
description: Creates directory structure based on a map.
version: 1.0.0
repository: https://github.com/flutter-cavalry/map_to_directory_structure

environment:
  sdk: ">=2.19.1 <3.0.0"

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
  directory_structure_map: ^1.2.0
  tmp_path: ^1.1.0
dependencies:
  convert: ^3.1.1
  path: ^1.8.3
